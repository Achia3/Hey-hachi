# Hachi AI - Python Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE on pyaudio (only needed by the legacy server-side mic/wakeword path;
# the main browser microphone + faster-whisper path does not require it):
#   Windows: pip install pipwin && pipwin install pyaudio
#   OR download a pre-built wheel from https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio

edge-tts>=6.1.9
openai-whisper>=20250625
imageio-ffmpeg>=0.6.0
pywebview>=5.0
flask>=3.0
speechrecognition>=3.10
psutil>=5.9
requests>=2.31
ollama>=0.6.2
pyaudio>=0.2.14; python_version < "3.14"
beautifulsoup4>=4.12.0
trafilatura>=2.0.0
ddgs>=9.14.0
python-dotenv>=1.0
json-repair>=0.50.0
pydantic>=2.7,<3
pypdf>=5.0
python-docx>=1.1
pillow>=10.0
pyautogui>=0.9.54
pyperclip>=1.9.0
pynput>=1.7.7
# Optional selectable local transcription engine. Set voice_stt_engine to
# "faster-whisper" in config.json to activate it.
faster-whisper>=1.1.0
pytest>=8.0.0
playwright>=1.49.0
